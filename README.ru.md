# Awesome Astra

**Компактные skills для Codex: нужный контекст, полноценные изменения и проверяемый результат.**

[English](README.md) · [Исследования](RESEARCH.md) · [Как индексирует Cursor](docs/CURSOR_INDEXING.md) · [Замеры](EVALUATION.md)

Два skills вместо огромного промпта и обязательной армии агентов:

| Skill | Назначение |
| --- | --- |
| `astra-code` | Разработка, отладка, рефакторинг, проектирование и ревью. |
| `astra-repo-audit` | Аудит и сокращение лишних инструкций без удаления защитных ограничений. |

В **0.7.0** добавлены приёмочные критерии, проверка выбранных рисков и привязка результатов к окружению. Исполняемые evals теперь включают миграции старых данных и повторную обработку событий. Для маленьких правок большой чеклист не требуется. **Экономия токенов и рост качества Astra пока не измерены.** Это не официальный продукт OpenAI.

## Согласованность изменений

Skill теперь требует проверить потребителей изменённого контракта и реальный
вход приложения. Добавлена карта Python-импортов на AST с кешированием по хешам:

```bash
python3 skills/astra-code/scripts/project_map.py --root /repo --source-root src --changed src/pkg/core.py
```

Карта возвращает транзитивных кандидатов-потребителей, хеш текущего состояния,
ошибки разбора и пробелы покрытия. Это синтаксические связи, не разрешение типов
компилятором и не универсальный граф вызовов. Другие языки и внешние потребители
помечаются как непроверенные. Изменения и удаления инвалидируют сохранённые факты.
`tools/check_integration.py` проверяет полноту рассмотрения потребителей и наличие
свидетельств проверки реального входа; формат — в [EVALUATION.md](EVALUATION.md).

## Установка

Нужен Python 3.10+. Для необязательного индекса — Git и SQLite с FTS5. Сторонние Python-пакеты, API-ключи, MCP и hooks не нужны.

```bash
git clone https://github.com/Paffin/Awesome-Astra-.git
cd Awesome-Astra-
python3 tools/validate.py
python3 -m unittest discover -s tests -v
python3 scripts/install.py
```

В названии репозитория действительно есть дефис в конце. Перед запуском прочитай скачанный код; для повторяемой установки закрепи проверенный commit.

По умолчанию устанавливается в `~/.agents/skills`. Только основной skill:

```bash
python3 scripts/install.py --skill astra-code
```

Для отдельного проекта используй `--dest /путь/к/проекту/.agents/skills`. Модель GPT-6 Astra выбирается в самом Codex; установщик не меняет настройки модели и разрешения.

При `--force` сначала готовится полная новая копия, затем заменяется установленный skill. Резервная копия хранится **рядом** с каталогом skills, в `.astra-skill-backups`, поэтому не создаёт ещё один обнаруживаемый skill. Одновременные установки блокируются lock-файлом. После аварийного завершения может потребоваться проверить и удалить оставшийся `.astra-install-*.lock`. Обновление транзакционно для каждого skill отдельно, не для всего набора сразу. Старые бэкапы, оставленные прежней версией внутри каталога skills, автоматически не удаляются.

Можно также попросить `$skill-installer` установить папки `skills/astra-code` и `skills/astra-repo-audit` из этого репозитория. Корневой `plugin.json` — переносимый manifest, `.codex-plugin/plugin.json` — совместимый manifest Codex; валидатор требует их полного совпадения. Публикация на GitHub не означает публикацию в каталоге плагинов.

## Работа

Полноценный устанавливаемый skill находится в
[skills/astra-code](skills/astra-code/SKILL.md): основной сценарий, UI-метаданные,
условные инструкции и все исполняемые инструменты. После установки исходный
репозиторий для работы не нужен. Вызов:

```text
$astra-code внедри изменение через существующий вход приложения, проверь
контракты и потребителей, подтверди интеграцию и перечисли пробелы покрытия.
```

Для языков кроме Python добавлен [общий клиент LSP](skills/astra-code/references/languages.md).
Он принимает команду сервера и ID любого языка с поддержкой references по stdio.
Это расширяемая поддержка протокола, а не заявление, что каждый сервер уже
протестирован. Автоматической установки серверов нет. Проверка протокола выполнена
на синтетическом сервере; пустой ответ не считается доказательством отсутствия связей.
Дополнительно пройдены живые проверки Python и TypeScript с определением и
потребителем в разных файлах. Версии и команды воспроизведения — в EVALUATION.md.

```text
$astra-code исправь гонку в worker и проверь исходную регрессию.
$astra-code проведи ревью текущей ветки относительно main.
$astra-repo-audit проверь AGENTS.md и skills; пока ничего не меняй.
```

Для опечатки не строится карта репозитория. Для сложной задачи подгружается нужный сценарий, а не вся библиотека инструкций. Параллельная работа допускается только при доступных инструментах, независимых задачах и разделении изменяемых файлов.

## Локальный индекс

Из каталога этого проекта:

```bash
python3 skills/astra-code/scripts/repo_index.py --root /путь/к/репозиторию search "invoice retry" --max-bytes 12000
```

Индекс обновляется перед поиском, учитывает удалённые и исключённые файлы и возвращает фрагменты с путём, диапазоном строк и SHA-256. Ограничение задаётся в байтах UTF-8 всего JSON-ответа, **не в токенах модели**.

Это взвешенный лексический поиск SQLite FTS5: символы и пути важнее обычного текста, сначала требуются все слова запроса, затем недостающие результаты дополняются явно отмеченными частичными совпадениями. Помимо Python AST используются консервативные границы объявлений для JavaScript/TypeScript, Go, Rust, Java/Kotlin/C# и shell; для остального остаётся построчное разбиение. Индекс не заменяет embeddings, полноценный граф зависимостей или семантический поиск Cursor. Каждый запуск перечитывает допустимые файлы для проверки хешей, но заново разбирает и записывает только изменившиеся.

После 0.2.0 один раз выполни `purge`: несовместимая старая схема отклоняется и не мигрирует молча.

Данные хранятся локально в метаданных Git отдельно для каждого worktree. Утилита не отправляет код в сеть. Однако передача найденного фрагмента облачной модели остаётся внешней передачей данных; исключения не заменяют корпоративную политику и сканер секретов.

Подробности, ограничения, исключения и очистка кэша: [CURSOR_INDEXING.md](docs/CURSOR_INDEXING.md).

## Проверка эффективности

`tools/compare_runs.py` сравнивает реальные парные замеры: входные, кешированные и выходные токены, время, принятие результата и безопасность. Незавершённая задача не считается улучшением только потому, что стоила дешевле. Формат данных и методика — в [EVALUATION.md](EVALUATION.md).

В наборе 24 сценария проверки поведения. Пока это спецификации испытаний, а не доказательство прохождения их моделью. Оценка размера инструкций также приблизительная и не равна счёту за API.

Лицензия [MIT](LICENSE). Источники и отличия от Ponytail, Superpowers, Agency Agents и autoresearch: [RESEARCH.md](RESEARCH.md).

## Проверяемый процесс

[Инструкции и команды](docs/VERIFIED_WORKFLOW.md): запись проверок с хешами исходников,
карта потребителей до и после правок, явные межъязыковые контракты и запуск
парных evals. `scripts/doctor.py` внутри установленного skill проверяет наличие
инструментов без запуска и установки языковых серверов.

[Проверки разработки 0.7.0](docs/DEVELOPMENT_CHECKS.md): критерии результата,
совместимость и восстановление, отказоустойчивость, внешние границы и окружение.
