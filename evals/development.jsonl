{"id": "acceptance-observable", "prompt": "Add an export option; preserve the existing public command and output format.", "expect": {"skill": "astra-code", "route": "implement", "must": ["define observable success and unchanged invariants", "verify the installed public command"], "must_not": ["equate implementation with acceptance"]}}
{"id": "migration-old-data", "prompt": "Add a required status field to existing persisted invoices; old app versions run during rollout.", "expect": {"skill": "astra-code", "route": "implement", "must": ["test historical records and mixed-version reads/writes", "record migration and recovery order"], "must_not": ["test only empty databases", "promise untested rollback"]}}
{"id": "retry-partial-failure", "prompt": "Fix duplicated jobs when an event is retried after worker failure.", "expect": {"skill": "astra-code", "route": "debug", "must": ["reproduce partial failure and duplicate delivery", "verify persisted effect count and idempotency"], "must_not": ["claim exactly-once from a retry counter"]}}
{"id": "concurrency-race", "prompt": "Prevent concurrent claims of the same job across workers.", "expect": {"skill": "astra-code", "route": "implement", "must": ["test competing operations at the durable boundary", "use deterministic concurrency coordination"], "must_not": ["rely solely on sequential calls or sleeps"]}}
{"id": "external-unavailable", "prompt": "Change an event schema consumed by an inaccessible billing repository.", "expect": {"skill": "astra-code", "route": "implement", "must": ["preserve compatibility or leave the external boundary unresolved"], "must_not": ["mark inaccessible consumer verified"]}}
{"id": "config-public-path", "prompt": "Enable a new worker option through our existing Helm values and app startup.", "expect": {"skill": "astra-code", "route": "implement", "must": ["trace effective configuration to behavior", "verify defaults and invalid values"], "must_not": ["add a setting that no consumer reads"]}}
{"id": "security-tenant", "prompt": "Add document download by id for tenant users.", "expect": {"skill": "astra-code", "route": "implement", "must": ["test authorized and cross-tenant denied requests", "check errors and logs for sensitive content"], "must_not": ["test only allowed access"]}}
{"id": "performance-query", "prompt": "Optimize the slow customer list without changing its results.", "expect": {"skill": "astra-code", "route": "implement", "must": ["measure realistic workload and query count", "record toolchain and dependency identity"], "must_not": ["claim capacity from one whole-command duration"]}}
{"id": "recovery-diagnostics", "prompt": "Make failed queue processing diagnosable and recoverable after restart.", "expect": {"skill": "astra-code", "route": "implement", "must": ["verify failure diagnostics without secrets", "test restart and user-visible recovery"], "must_not": ["equate alert configuration with effective detection"]}}
{"id": "journey-accessibility", "prompt": "Add retry to the failed upload screen.", "expect": {"skill": "astra-code", "route": "implement", "must": ["exercise real upload error and retry path", "preserve keyboard focus and accessible controls"], "must_not": ["accept a static component snapshot as full user-journey proof"]}}
{"id": "environment-drift", "prompt": "The same source now fails after changing a lockfile outside the repo. Validate the previous check evidence.", "expect": {"skill": "astra-code", "route": "debug", "must": ["revalidate explicitly declared environment inputs", "reject stale receipts"], "must_not": ["trust source hash alone"]}}
{"id": "small-edit-no-ceremony", "prompt": "Correct the misspelling in this static button label.", "expect": {"skill": "astra-code", "route": "implement", "must": ["make and verify the local edit directly"], "must_not": ["require migration and concurrency checklists", "create acceptance ledgers without a relevant risk"]}}
