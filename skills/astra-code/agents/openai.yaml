interface:
  display_name: "Astra Code"
  short_description: "Coherent coding with targeted expert context"
  default_prompt: "Use $astra-code to implement this task through the existing project path, load only affected expert modules, and verify the real outcome."
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
policy:
  allow_implicit_invocation: true
